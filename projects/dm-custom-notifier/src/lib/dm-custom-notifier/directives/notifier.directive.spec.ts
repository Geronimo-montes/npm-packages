import { DMNotifierDirective } from './notifier.directive';

describe('NotifierDirective', () => {
  it('should create an instance', () => {
    const directive = new DMNotifierDirective();
    expect(directive).toBeTruthy();
  });
});
