import { DMOnClickNotifierDirective } from './on-click-notifier.directive';

describe('OnClickNotifierDirective', () => {
  it('should create an instance', () => {
    const directive = new DMOnClickNotifierDirective();
    expect(directive).toBeTruthy();
  });
});
