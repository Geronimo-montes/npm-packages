import { DMOnCreateNotifierDirective } from './on-create-notifier.directive';

describe('NotifierDirective', () => {
  it('should create an instance', () => {
    const directive = new DMOnCreateNotifierDirective();
    expect(directive).toBeTruthy();
  });
});
