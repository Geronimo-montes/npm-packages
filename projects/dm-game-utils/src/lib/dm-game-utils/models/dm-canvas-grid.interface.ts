/**
 * Interface representing the global configuration for rendering on the canvas.
 */
export interface DMCanvasConfig {
  pixel: number;
  heightGrid: number;
  widthGrid: number;
  heightCanvas: number;
  widthCanvas: number;
}
