import { DmRestApiHelperDirective } from './dm-rest-api-helper.directive';

describe('DmRestApiHelperDirective', () => {
  it('should create an instance', () => {
    const directive = new DmRestApiHelperDirective();
    expect(directive).toBeTruthy();
  });
});
