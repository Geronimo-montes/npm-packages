import { DmRestApiHelperPipe } from './dm-rest-api-helper.pipe';

describe('DmRestApiHelperPipe', () => {
  it('create an instance', () => {
    const pipe = new DmRestApiHelperPipe();
    expect(pipe).toBeTruthy();
  });
});
