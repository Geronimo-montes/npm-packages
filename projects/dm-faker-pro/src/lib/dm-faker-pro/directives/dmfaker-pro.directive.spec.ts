import { DMFakerProDirective } from './dmfaker-pro.directive';

describe('DMFakerProDirective', () => {
  it('should create an instance', () => {
    const directive = new DMFakerProDirective();
    expect(directive).toBeTruthy();
  });
});
